<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Favourites</title>
    <link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="main.scss">
</head>

<body>
    <div class="flex flex-col min-h-screen overflow-hidden">

        <%- include('partials/header'); %>
        <main class="flex-grow">

            <section class="bg-gradient-to-b from-gray-100 to-white">
                <div class="max-w-6xl mx-auto px-4 sm:px-6">
                    <div class="pt-32 pb-12 md:pt-40 md:pb-20 text-center">

                        <div class="max-w-3xl mx-auto text-center pb-12 md:pb-20">
                            <h1 class="h1">Favourite Pays</h1>
                        </div>
                        <div class="flex flex-wrap -mx-3 mt-6">
                            <div class="w-full px-3">
                                <button class="btn text-white bg-blue-600 hover:bg-blue-700 w-6/12" type="submit" onclick="location.href='http://localhost:3000/home/fundtransfer/favourite/addfav/getdata'">Add Favourites</button>
                            </div>
                        </div>
                        <form action="/home/funtransfer/favourite/deladd" method="post">
                            <div class="flex flex-wrap -mx-3 mb-4">
                                <div class="w-full px-3">
                                    <% for(var i=0; i<articles.length; i++){ %>
                                        <p >
                                            <%= articles[i].fav_id %> - <%= articles[i].payee_nick_name %> - <%= articles[i].account_to_account_number %> - <%= articles[i].transaction_amount %>
                                            <button class="btn text-white bg-blue-600 hover:bg-blue-700 w-1/6" type="submit" value="<%= articles[i].fav_id %>" name="pay">Pay</button>
                                            <button class="btn text-white bg-blue-600 hover:bg-blue-700 w-1/6" type="submit" value="<%= articles[i].fav_id %>" name="delete">Delete</button>
                                        </p>
                                        <br>
                                    <% } %>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </section>

        </main>

    </div>
</body>

</html>